# core/words — `@lexiai/core-words`

Word use cases (GetWord, EnqueueGenerateWord, ApplyJobResult).

- **May import:** `repositories/*`, `@lexiai/*` packages, `effect`.
- **MUST NOT import:** `apps/*`. No HTTP code — that lives in `apps/api`.
