# platform/observability — `@kotodama/platform/observability`

`TracingLive(serviceName)` — OpenTelemetry Layer factory. Everything binding (local/prod wiring, why
OTel over Effect DevTools, the `process.env` leaf-adapter constraint, span conventions) lives in
observability.md, which is path-scoped to this subtree and auto-loads.
