{
  "name": "@lexiai/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "echo 'TODO(db:generate): drizzle-kit generate — wired in a downstream feature'",
    "db:migrate": "echo 'TODO(db:migrate): drizzle-kit migrate'",
    "db:push": "echo 'TODO(db:push): drizzle-kit push'",
    "db:reset": "echo 'TODO(db:reset): drop + recreate + migrate'",
    "db:seed": "echo 'TODO(db:seed): seed dev data'",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "catalog:effect",
    "@effect/sql-pg": "catalog:effect",
    "drizzle-orm": "catalog:db",
    "drizzle-kit": "catalog:db"
  }
}
