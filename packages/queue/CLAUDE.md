# packages/queue — `@lexiai/queue`

`QueueService` over `@aws-sdk/client-sqs` (SQS Layer). **Backend-only.**

- **May import:** `effect`, `@aws-sdk/client-sqs`.
- **MUST NOT** be imported by `apps/web`.
- Scaffolding only.
