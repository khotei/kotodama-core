# packages/ai — `@lexiai/ai`

`AiService` wrapping `@effect/ai-openai` (text + image). **Backend-only.**

- **May import:** `effect`, `@effect/ai-openai`.
- **MUST NOT** be imported by `apps/web`.
- Scaffolding only — real impl is a downstream feature.
